// Write a program to calculate perimeter of rectangle.Take sides, a & b, from the user.

#include<stdio.h>

int main (){
    int a, b; // diclaration of two variables a & b
    printf("Enter a : "); 
    scanf("%d", &a); // take input of a from user
    printf("Enter b : ");
    scanf("%d", &b); // take input of b from user 
    printf("Perimeter of Rectangle is : %d", 2*(a+b)); // calculate and print perimeter of rectangle
 
    /*this program is for calculate perimeter of rectangle and print it 
    and this program also indicate use of comments, both types comments single line comment and multi-line comment
    this prigram also include chepter 1 example 3 */
    
    return 0;
}