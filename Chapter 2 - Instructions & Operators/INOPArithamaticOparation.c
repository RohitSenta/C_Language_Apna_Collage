// Arithamatix Instruction Oparation 
//Note : Single variable on LHS

#include<stdio.h>

int main() {
    int a = 5, b = 6;
    int sum = a + b;
    int multiply = a * b; //Note : Single variable on LHS

    printf("%d\n", sum);
    printf("%d\n", multiply);

    return 0;
}